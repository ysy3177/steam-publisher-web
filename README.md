# Steam Publisher Web v0.2.0

기존 Windows 매크로를 복사하거나 수정하지 않고, 사용자 요구사항을 기준으로 새로 작성한 독립 웹 프로젝트입니다.

## 현재 기능
- DOCX 브라우저 내 분석
- KR / JP / EN / TW / CN / TH 언어 구간 분리
- RU = EN 매핑
- 언어별 제목/본문 미리보기
- DOCX 이미지/표 HTML 변환
- Steam 편집기 연결용 북마크 도우미
- Steam 편집기 읽기 전용 안전 진단

## v0.2.0 안전 진단에서 하는 것
- 편집 가능한 영역 탐색
- 제목 입력 후보 탐색
- 언어 선택 UI 후보 탐색
- 저장 버튼 후보 탐색
- 표시 중인 이미지 탐색
- 진단 결과를 Steam Publisher Web 탭으로 전송

## v0.2.0에서 하지 않는 것
- 제목 수정
- 본문 삭제/입력
- 이미지 삭제/교체
- 저장 버튼 클릭
- 공지 복제/발행

## 배너 보호 규칙
Steam 공지를 복제한 뒤 기존 상단 배너와 하단 배너는 유지합니다.
실제 자동화에서는 두 배너 사이의 기존 본문만 제거하고 DOCX의 해당 언어 본문을 삽입합니다.

## 사용 순서
1. DOCX를 분석합니다.
2. `Steam 연결 도우미` 버튼을 브라우저 북마크바로 끌어다 놓습니다.
3. Steam 공지 편집 URL을 넣고 `Steam 편집 페이지 열기`를 누릅니다.
4. 새로 열린 Steam 탭에서 북마크바의 `Steam 연결 도우미`를 클릭합니다.
5. 다시 웹 도구로 돌아와 `안전 진단 실행`을 누릅니다.
6. 결과를 확인한 뒤 실제 자동화용 선택자를 확정합니다.


## v0.2.1
- DOCX의 상단 배너 자동 제외
- DOCX의 하단 배너 자동 제외
- `< 아래 내용 제외 >` / `＜아래 내용 제외＞` 문구 이후 전체 자동 제외
- Word의 빈 문단을 실제 공백 줄로 미리보기에 유지
- 실제 Steam에 삽입할 "배너 사이 본문"만 미리보기에 표시


## v0.2.2
- Mammoth가 버리던 Word의 실제 빈 문단을 `word/document.xml`에서 직접 확인해 복구
- 인삿말 / 본문 / 참고사항 사이에 원문에 존재하는 한 줄 공백을 미리보기에 다시 삽입
- 배너 제외 및 `< 아래 내용 제외 >` 이후 제외 규칙은 그대로 유지


## v0.3.0
- 실패 가능성이 높았던 북마크 `javascript:` 연결 방식 제거
- Chrome 확장 프로그램 Bridge 방식 추가
- 동일한 Chrome 로그인 세션의 Steam 탭을 DOM으로 직접 읽을 수 있음
- Windows 마우스/키보드 API를 사용하지 않음
- 현재는 읽기 전용 안전 진단만 제공
- 향후 동일한 확장 프로그램을 이용해 제목/본문/언어/저장을 백그라운드 탭에서 처리할 예정


## v0.3.1
- 연결 실패 지점을 4단계로 화면에 표시
  1. 웹페이지 연결
  2. 확장 프로그램 실행
  3. Steam 탭 발견
  4. Steam 편집 화면 확인
- Render 호스트 매칭 범위를 보강
- window.postMessage + DOM attribute observer의 이중 연결 경로 사용
- 여전히 Steam 내용 변경/저장/게시 기능 없음 (읽기 전용)

## v0.3.2
- v0.3.1에서 `scanBtn` / `scanResult`가 app.js 안에 중복 선언되어 전체 JavaScript가 시작되지 않던 오류 수정
- 사용하지 않는 구형 북마크 연결 변수 제거
- 연결 진단 버튼 이벤트가 정상 등록되도록 수정
- Steam 변경/저장/게시 기능은 여전히 없음 (읽기 전용)

## v0.4.0
- 실제 Steam 안전 진단 결과를 바탕으로 편집기 구조 인식
- 제목 입력: `placeholder="여기에 이벤트 제목 입력"`
- 본문 편집기: `.ProseMirror[contenteditable="true"]`
- 언어 select 탐지
- 저장 버튼 탐지
- 본문 편집기 내부의 큰 이미지 중 첫 번째/마지막 이미지를 상단/하단 배너 후보로 인식
- `배너 경계 확인` 버튼 추가
- Steam 페이지에는 임시 초록 테두리/라벨만 표시하며 30초 후 자동 제거
- 내용 변경, 저장, 게시 기능 없음

## v0.4.1
- Steam 탭에 content script가 없을 때 자동으로 `steam-content.js`를 주입하도록 변경
- 확장 프로그램 업데이트 후 Steam 탭을 수동 새로고침하지 않아도 안전 진단/배너 경계 확인 가능
- `Could not establish connection. Receiving end does not exist.` 오류 대응
- 중복 주입 방지 가드 추가
- 여전히 내용 변경/저장/게시 기능 없음

## v0.5.0
- 테스트용 복제 공지에서 한국어 1개 언어만 미저장 교체 테스트
- 현재 Steam 언어가 한국어일 때만 실행
- 기존 상단/하단 배너 블록은 그대로 보존
- 두 배너 사이의 기존 노드만 제거 후 DOCX KR 본문 삽입
- 제목 입력값도 DOCX KR 제목으로 임시 교체
- 저장/게시 버튼은 절대 클릭하지 않음
- 큰 배너 이미지가 정확히 2개가 아니면 안전 중단

## v0.5.1
- 한국어 미저장 테스트에서 DOCX 본문 필드명을 잘못 참조하던 오류 수정
- `kr.body / kr.html` → 실제 파서 필드인 `kr.bodyHtml` 사용
- 저장/게시 기능은 여전히 실행하지 않음

## v0.5.2
- `app.js?v=052` 캐시 버스터 추가: 새 배포 후 이전 app.js가 남는 문제 방지
- 한국어 미저장 테스트 전 웹앱에서 title/bodyHtml 길이를 직접 검증
- 전송 직전 본문 HTML 길이를 화면에 표시
- 확장 프로그램 전달 단계에서도 payload를 문자열로 강제 정규화
- 저장/게시 동작 없음

## v0.5.3
- 한국어 미저장 테스트 버튼 활성화 조건 수정
- 기존에는 연결 진단 시점에 `parsed.KR`가 없으면 비활성화되고, 이후 DOCX를 읽어도 상태가 갱신되지 않았음
- 이제 Steam 연결만 정상이라면 버튼 활성화
- DOCX가 없을 경우 버튼 클릭 시 안내하고 중단
- app.js 캐시 버스터를 v053으로 갱신
- 저장/게시 동작 없음

## v0.5.4
- Steam에 넣을 때 DOCX의 실제 빈 문단이 사라지던 문제 수정
- 웹 미리보기용 `.docx-real-gap` / `.docx-empty-line`을 Steam ProseMirror용 `<p><br></p>`로 변환
- 일반 빈 `<p>`도 `<br>`를 넣어 실제 한 줄 공백으로 유지
- 결과 JSON에 `insertedBlankParagraphs` 수 표시
- 저장/게시 동작 없음

## v0.5.5
- 본문 마지막 문단과 하단 배너 사이에 최소 1줄 공백을 항상 보장
- DOCX에 이미 마지막 빈 줄이 있으면 그대로 사용
- 없으면 Steam ProseMirror용 `<p><br></p>` 1개를 자동 삽입
- 결과 JSON에 `insertedFinalGapBeforeBottomBanner` 표시
- 저장/게시 동작 없음

## v0.6.0
- 7개 언어 미저장 순환 테스트 추가
- 순서: KR → EN → JP → CN → TW → TH → RU
- RU는 EN 제목/본문을 재사용
- Steam의 실제 언어 select를 DOM 이벤트로 전환하고 각 언어 로딩을 기다린 뒤 적용
- 각 언어마다 상단/하단 배너 2개를 다시 탐지해 그 사이만 교체
- 공백 문단 및 하단 배너 직전 1줄 공백 유지
- 중간 실패 시 즉시 중단하고 완료된 언어 목록을 결과에 반환
- 마지막에는 한국어 화면으로 복귀
- 저장/게시 버튼은 호출하지 않음

## v0.6.1
- 7개 언어 순환 시작 시 KR에서 배너 1개만 감지되던 문제 대응
- 이미 선택된 언어(KR)에는 불필요한 change 이벤트를 보내지 않음
- 언어 전환 후 편집기와 배너 2개가 모두 준비될 때까지 최대 10초 대기
- 배너 판정 시 렌더링 크기뿐 아니라 naturalWidth/naturalHeight와 width/height 속성도 사용
- 실패 시 이미지 개수/크기 디버그 정보 반환
- 저장/게시 동작 없음

## v0.6.2
- 웹페이지에 순환 테스트 상태 박스 추가: 대기 / 진행 중 / 완료 / 중단
- 완료 시 `7/7 언어 적용 완료 · 저장/게시하지 않음` 표시
- DOCX 원본 XML의 실제 문단 순서를 추가로 읽어, Mammoth가 인접 문단을 한 문단으로 합친 경우 복구 시도
- 사용자가 보여준 `- 심연의 제단...` 다음 `ㄴ ...` 문장이 붙는 유형 대응
- 기존 빈 문단 복구/배너 보존/미저장 동작 유지

## v0.6.3
- 문제 원인 수정: 해당 DOCX의 두 문장은 서로 다른 문단이 아니라 한 Word 문단 안의 `w:br` 수동 줄바꿈이었음
- `word/document.xml`에서 `w:br` 위치를 직접 읽어 웹 HTML에 `<br>`로 복원
- 예: `- 심연의 제단... [하드 모드]` 다음 `└ ... 이벤트가 종료되며...`를 별도 줄로 유지
- 기존 실제 빈 문단 복구와 7개 언어 미저장 순환 테스트 유지
- 저장/게시 동작 없음

## v0.6.4
- DOCX 구분선 복구 추가
  - Word 문단의 실제 top/bottom border 감지
  - 이미지가 아닌 DrawingML/VML 선 도형 감지
  - 웹 미리보기와 Steam 삽입 시 `<hr>`로 복원
- 표 정규화 추가
  - 첫 행을 `<th>` 헤더로 변환
  - 셀 테두리/패딩/정렬을 Steam 편집기에서 보기 좋게 정리
  - 셀 내부 Word `<p>` 여백 제거
- 기존 줄바꿈/빈 문단/배너 보존/7개 언어 미저장 테스트 유지
- 저장/게시 동작 없음


## v0.6.5
- 제목 추출을 DOCX XML 기준으로 변경: 각 언어 마커 뒤 첫 배너 직전의 마지막 텍스트 문단을 제목으로 사용
- 제목 80자 초과/줄바꿈 포함 시 다국어 테스트 시작 전 안전 중단
- 언어 전환 후 제목이 늦은 Steam 재렌더링으로 되돌아가는지 검증하고 1회 재적용
- 구분선 탐지 보강: 직접 문단 border + style 상속 border + Drawing/VML 선 + 빈 1x1 table 형태
- 모든 구분선 뒤 공백을 정확히 1줄로 정규화
- 표는 Word 인라인 스타일을 제거하고 Steam native table 구조(table/tr/th/td)만 전달
- 웹 미리보기의 표 색/테두리/패딩을 Steam 형태에 가깝게 정리
- Word의 반복 공백/앞뒤 공백/탭을 NBSP로 변환하여 2칸 공백이 사라지지 않게 처리
- Word paragraph indent를 보수적으로 NBSP 들여쓰기로 복원
- 저장/게시 동작 없음

## v0.6.6
- 제목/본문 경계 로직 재작성
- 제목은 언어 마커 바로 다음 첫 번째 비어있지 않은 Word 문단만 사용
- 더 이상 '상단 배너 직전 마지막 텍스트'를 제목으로 사용하지 않음
- 본문 추출을 제목 위치와 완전히 분리
- 상단 배너와 하단 배너를 먼저 찾고, 두 배너 사이 내용 전체를 본문으로 사용
- 제목 파싱이 잘못되어도 배너 이후 초반 본문이 사라지지 않도록 수정
- 간체 제목에 본문이 섞이거나, 배너 이후 앞부분 내용이 유실되던 회귀 버그 수정
- 기존 구분선/표/공백/들여쓰기 보정은 유지
- 저장/게시 동작 없음

## v0.7.0
- DOCX 해석기를 Mammoth 보정 방식에서 `word/document.xml` 직접 해석 방식으로 교체
- 제목: 언어 마커 이후 최초 텍스트 구간을 제목으로 인식
- CN처럼 `제목 + 구분선 + 본문`이 한 Word 문단 안에 있어도 분리
- Word 빈 문단 개수를 그대로 유지해 공백 0/1/2줄을 구분
- VML/DrawingML 선을 `<hr>` 구분선으로 복원
- Word 표를 행/셀 구조 그대로 HTML table로 변환하고 Steam용 스타일 적용
- 본문 이미지 유지
- Steam 기존 상/하단 배너는 계속 보존
- UI 정리:
  - 최초 1회 extension 등록 안내 + 등록 방법 추가
  - 진단/테스트 버튼과 JSON 결과 숨김
  - "다음 단계" 제거
  - `Steam 공지 적용` 버튼만 노출
  - 실제 적용 오류는 팝업으로 표시
- 아직 저장/게시 자동화는 하지 않음
